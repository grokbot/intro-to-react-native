import { StyleSheet } from "react-native";
import { Colors } from "../common";

export const OverridesAndroid = StyleSheet.create({
	main: {
		backgroundColor: Colors.red
	},
	loader: {
		backgroundColor: Colors.red
	}
});
