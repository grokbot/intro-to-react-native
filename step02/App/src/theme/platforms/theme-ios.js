import { StyleSheet } from "react-native";
import { Colors } from "../common";

export const OverridesIOS = StyleSheet.create({
	main: {
		backgroundColor: Colors.blue
	},
	loader: {
		backgroundColor: Colors.blue
	}
});
